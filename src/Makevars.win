RTOOLS_VERSION = 2.9

NPSOL_DIR= "..\inst\npsol\windows\x86\rtools"

PKG_LIBS=$(FLIBS) $(BLAS_LIBS) $(LAPACK_LIBS) -lnpsol -L$(NPSOL_DIR)$(RTOOLS_VERSION)

# Uncomment the next line to turn on all debugging output in the backend
# PKG_CFLAGS=-DDEBUGMX
